# Maintained by Chef, do not manually edit this file as changes will not be persistent
filter {
    if [type] == "Generic-Syslog" {
        syslog_pri { }
        date {
            match => [ "syslog_timestamp", "MMM  d HH:mm:ss", "MMM dd HH:mm:ss", "MMM dd yyyy HH:mm:ss", "MMM d yyyy HH:mm:ss" ]
            timezone => "America/Anchorage"
        }
            mutate {
            rename => [ "syslog_severity", "Severity" ]
        }
    }
}